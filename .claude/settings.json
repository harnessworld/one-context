{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '/(meta|\\.\\.?claude/agents|knowledge)/' && onecxt adapt --all --only claude_code 2>/dev/null || true; }",
            "timeout": 30,
            "statusMessage": "Syncing CLAUDE.md via onecxt adapt..."
          }
        ]
      }
    ]
  }
}
