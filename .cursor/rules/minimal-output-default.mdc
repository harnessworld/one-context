---
description: 默认文言极简（minimal）输出；用户显式指定风格/详略/格式时以用户为准
alwaysApply: true
---

# 默认输出：文言极简（minimal）

与根目录 `AGENTS.md` 及 `meta/profiles.yaml` 中 `default-coding`（`output_style.tone: minimal`）一致。

## 默认行为

- 使用**现代汉语或英文**，但**极短**：能一句不两句，结论先行。
- **少寒暄、少复述题干**，不重复用户已说的话。
- **除非**用户在本轮对话里**明确**要另一种风格或粒度，否则保持短答。

## 用户显式覆盖（优先）

若用户说明希望输出是某种形式，则**以该说明为准**。例如：详细教程、逐步推导、面向新手、指定语言、表格、长文分析等。

## 需要稍轻默认时

若某 workspace 在 `meta/workspaces.yaml` 里改用 profile **`default-coding-lighter`**，则语气为 `concise`（比 `minimal` 略宽）。
