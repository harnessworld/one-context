# aips-personal (Python package)

Install from the aips-personal root directory (repository root that contains `meta/repos.yaml`):

```bash
pip install -e ./packages/aips-personal
```

The `aiws` command is the main CLI. See the repository root `README.md` for product documentation.
