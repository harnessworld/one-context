# 口播讲稿

<!-- 由 WAV 音频自动转写生成，经繁简转换与基础校对 -->

## Prompt Caching 是 Agent 产品的架构基础

哈喽,大家好,欢迎收听我们的播客。今天想跟大家聊一聊,在做长上下文的agent的时候,为什么caching是不可或缺的。这个其实是受到了Anthropic最近的一个宣布,就是他们的Claude Code通过使用了prompt caching之后,成本和延迟都下降了一个数量级。

对,这个确实挺让人惊讶的。所以我们就来聊一聊caching到底在长上下文的这种应用当中扮演了一个什么样的角色。

我们先来聊第一部分,就是缓存机制与结构,就是prompt caching它的底层逻辑到底是什么。我觉得这个是非常重要的。首先第一个问题就是,API缓存它的前缀匹配机制到底是怎么工作的。其实它缓存的是你输入的token序列的一个中间状态。然后它是用一种精确哈希匹配的方式。

对,它不是那种模糊匹配,它一定是从第一个token开始到你指定的那个断点全部都一样,它才会认为是同一个缓存。那就是说只要前面有一点点不一样,后面就全都要重新算。对,就是这样。所以说这个顺序就非常重要。就你这个tools system messages这个顺序一旦定下来之后,你就只能在最后面追加新的东西。

你不能说我在中间改,或者说我这个东西换顺序了,那整个缓存就全部都失效了。所以这个就是为什么要让你最稳定的东西放在最前面。这样的话可以最大限度的去复用你之前的计算结果。那就是说这个Claude Code它到底是怎么把提示分层的,然后又怎么用到这个缓存里的呢?它是一个三层的一个缓存的结构,最底层放的是全局都不会怎么变的一些系统的提示和工具的定义。

然后这是大家都共享的。再往上一层是每个项目自己独有的一些配置。最上面一层是每个对话自己的一些动态的历史。那这个就是每一层都只缓存自己关心的东西。那这样的话就可以减少一些重复的计算。那这个分层之后怎么保证说我每次都能精准的命中我想要的那个缓存呢?它是通过这个cache control这个标签来做一个断点。

然后它会自动的帮你检查说你这个最长的匹配的前缀是哪个?你可以去指定最多四个断点嘛。那它就会从最长的那个匹配的地方开始给你读缓存。那这样的话就可以保证你这个每一个部分的这个更新都不会影响到其他的部分。

最大限度的去利用了这个缓存又保证了这个灵活性和隔离性。那在实际应用这个prompt caching的时候有哪些比较容易被大家忽略的一些最佳实践或者说一些小陷阱呢?比如说像这个把动态的内容比如说时间戳或者说一些变化很频繁的一些参数放在了这个很容易被缓存的这个部分。就会导致你这个缓存几乎是无效的。就是你要把这些东西单独拎出来不要让它污染了你的这个稳定的前缀。

然后包括像这个系统的提示尽量不要去动态的修改它。那如果说你真的需要传递一些变化的信息的话,你可以通过这个messages这个参数来传递。这样的话就能更精准的去命中缓存。对,没错没错。然后还有就是比如说像这个工具的这个定义的顺序啊一定要保持一致。包括你这个如果是团队协作的话一定要把这个公共的配置啊都统一好。

还有就是要监控这个缓存的命中率啊包括你在做一些比如说像对话的压缩啊或者是说这个会话的这个branching啊。就是你要特别小心不要破坏了这个原有的这个前缀。然后咱们来聊第二部分就是操作的要点和陷阱。就是说这个在实际使用caching的时候有哪些常见的误区和对应的最佳实践。最常见的一个误区就是你会在运行的过程当中去修改这个system prompt。

对。那这个就会导致你的这个缓存全部都失效。那你正确的做法其实是把你想要的这个动态的信息放到user message或者是说这个tool results里面去传递。那这样的话就不会影响你的这个缓存的前缀。哦原来是这个传递的方式也会影响到这个缓存的命中率。对,没错没错。对,然后还有一个就是说你这个工具的这个增减或者说这个换顺序其实也是会导致你的这个缓存失效的。

那你可以用一个就是统一的这个工具的列表里面有一些就是标记位来控制它的这个状态。那还有就是说这个模型的切换也会导致你的这个缓存失效。那你可以用这个子智能体来做这个分叉的这个处理,就可以避免这个问题。

就是让每一个这个叫什么对话的路径它都是可以独立的去缓存的。那这个在做这个长上下文的这个caching的时候这个上下文的压缩有哪些关键的细节需要注意呢?就是你在压缩的时候你一定要保证你的这个系统的提示和你的这个工具的定义是跟你原来的那个会话是一模一样的。然后你通过一个特殊的这个压缩的消息类型来进行一个标识。这样的话它才能够正确的去命中这个缓存。

你也可以通过这个只留最近的几个工具的结果或者说你把这个大的输出持久化到外部在本地维护一个压缩的状态来帮你做这个更精细的控制。听起来好像这个压缩的这个策略的设计还是挺有讲究的。没错没错对不对对。因为如果你这个策略做得不好的话就很容易出现这种摘要丢失关键信息啊或者说这个模型的性能下降啊或者说这个缓存命中率低啊等等的问题。

那你可以通过主动的触发压缩啊,然后给这个压缩加上一些自定义的描述啊,用这个紧凑的消息结构啊,包括去监控这个缓存的命中率啊等等的一些手段来提升你的这个整体的效率和稳定性。

我们在做这个实际的工程化落地的时候,在使用Prompt Caching这个技术的时候,有哪些比较实用的建议可以帮我们去避免一些常见的坑呢?最核心的就是要把你的这个System Prompt和你的这个工具的定义放在最前面,而且保证他们是不会变的,然后把你的这个动态的信息都放到这个Messages里面,并且通过这个Cache Control这个断点来做一些分层的处理。

你要去监控你的这个缓存的命中率,如果这个命中率有异常的波动的话,往往就意味着你的这个。缓存的结构或者说你的这个流程上面出现了一些问题,那你就要去调整你的这个策略,所以说这个结构设计和这个监控都是缺一不可的,对,而且就是要尽量的去避免在这个中间去更换你的这个模型,或者说更换你的这个工具集,然后要合理的使用这个。

子智能体和这个压缩的机制啊,那你这个系统才会比较高效比较稳定,如果你违反了这些原则的话,比如说你随意的去改变了你的这个前缀啊,或者说你这个压缩的时候丢失了一些关键的状态啊,都有可能导致你的这个缓存彻底失效,那你的这个系统的开销就会暴增,所以说这些细节都是非常重要的,那么接下来要聊的就是这个设计原则的总结了,那这部分就想请你分享一下,你认为在做这个Prompt Caching的架构设计的时候,最需要关注的一些核心的要点,就是你要让这个Caching真正的高效,那首先第一点就是你要把你的这个System的部分和这个Tool的定义都作为一个只读的前缀,OK,对,然后这个东西一定是大家共享的,不变的,动态的内容一定是放到后面,而且你最好是通过一些Cache Control这个断点啊,把它分层,这样的话可以最大化的去复用,所以结构的分层和这个内容的隔离其实是很重要的,对,对,没错没错,然后就是缓存的这个键,你要能够去精准的区分,不同的用户,不同的会话,包括你要去有一些敏感的信息,你要能够去识别出来,不要放到这个共享的缓存里面,你的这个缓存的更新,你要能够去支持这种版本化的管理和这种主动的失效,甚至你可能需要有一些这种多级的缓存和这个语意的解锁,你才能够去扛住这种高并发你的这个监控一定是要覆盖到这个命中率啊,延迟啊,包括这个存储的使用啊,等等这些关键的指标你才能够真正的让这个架构是安全的是高效的对,那我们就想请你聊一聊,在你看来要想做好这个Prompt Caching的这个状态管理和这个更新的机制哪些原则是最核心的就是我觉得要让这个Caching真正的有效你最核心的就是要把你的这个System和这个Tool的这个定义作为一个只读的头部,然后永远都不要去动态的修改它所有的变化的部分都应该通过User Message或者是Tool Result这种方式来进行一个追加,这样的话你才能够保证这个缓存的这个前缀,它是稳定的,可以被复用到的哦,原来是要彻底的隔离这个静态和动态的内容,对,而且就是这个缓存的更新,你要能够去支持这种版本化的管理和这种主动的失效,对,就是你可以通过比如说这个模型的版本或者是这个工具的版本作为这个缓存的键的一部分然后配合上这种事件驱动的这种机制可以去批量的清除,或者是说你可以定期的去扫描加上这种TTL,你可以去设置不同的这个缓存的时间对于不同的内容,包括你可能需要有一些这种对于敏感信息的一些特殊的处理,就是你要能够保证这个数据的安全性和这个时效性是同时都可以做到的你觉得在实际生产环境当中,要想做好这个prompt caching的监控和评估有哪些关键的指标是我们必须要关注的首先最核心的就是你要去追踪这个缓存的命中率,这个是你要去追踪的一个核心的指标然后这个命中率我们一般的话是要保证在60%以上如果是那种特别特别高并发的场景的话,可能要到90%以上,你才算是OK的你还要去看这个平均的缓存的延迟,这个一般的话我们是要控制在5毫秒以内你才能够保证说你的这个整体的性能是没有被拖累的哦,原来还需要关注这个存储的使用情况对,当然啊,就是你这个存储的用量不能超过这个系统的这个上限,比如说你的内存或者说你的这个Redis的这个实例,它的这个上限然后配合上一些这种告警的规则比如说你的命中率下降了,或者说你的这个延迟突然之间飙升了,或者说你的这个存储的用量快要满了等等的一些情况,你可以去及时的发现说OK,我这个系统是不是出现了一些问题包括你可以去做一些这种缓存的预热,就是在上限之前把一些热门的内容提前加载进去你可以去做一些这种AB测试,对比有缓存和无缓存的效果,最终你可以去准确的评估说OK,我这个缓存到底给我带来了多少的收益,对,今天我们把这个缓存的这个架构的设计的一些细节和一些最佳实践都给大家聊了一遍,嗯,对,然后也希望能够激发大家去尝试着在自己的这个agent当中去应用这些思路,嗯,对,也欢迎大家能够来跟我们分享你们的一些设计和实践的经验,这是本期节目啦,感谢大家的收听,咱们下期再见,拜拜
