# 翻页时刻 — 语义契约（模板）

在 **`node cli.js wav`** 或流水线 **Step 11** 之前，把本文件复制为 **`timing/flip-boundaries.md`** 并填完。**`timing-check` 只防「句首陷阱」，不防「话题已在下一页但画面还在上一页」**——这张表是人的语义门控。

## 填写规则

1. **进入第 n 页的音频时刻**：从音轨 `0:00` 起的秒数（与 `wav-durations.json` 前缀和一致）。
2. **锚点**：优先写 **`sub.srt` 条目序号** + 该条 **起始时间码** + **起始几个字**；翻页时刻应对齐「**下一页话题已独立成句**」的起点，而非过渡语起点（见 SKILL.md「翻页边界语义」）。
3. 每一行都要能回答：**听众在这一秒听到的核心命题，与当前页 `data-title`/幻灯主题是否一致**。
4. **观感与「均匀」**：`timing-check` 与语义锚点**不保证**每页时长均匀。若相邻锚点间隔 **&lt; 约 5s**，口播仍在同一认知节拍内，宜 **合并为同一页幻灯**，避免闪页；若单页锚点跨度 **&gt; ~60s** 且中间有明显子话题转折，可 **拆成两页** 以免单屏过久（拆页仍须逐句对齐 SRT）。

---

**音轨**：`media/<your>.wav`（ffprobe 实测时长 ____ s）  
**字幕**：`subtitles/sub.srt`（或项目约定的 `srtFile`）

| 页码 | 幻灯标识（如 data-title） | 进入该页的音频时刻 (s) | 锚点（SRT # / 时间码 / 起始文本） |
|------|---------------------------|-------------------------|-------------------------------------|
| 1 | Cover | 0.000 | （起点） |
| 2 | … | … | … |
| … | … | … | … |

**闭合**：最后一页持续到音轨结束；最后一项 `slideDurationsSec` = 尾时长。

## 变更记录

| 日期 | 变更 |
|------|------|
| YYYY-MM-DD | 初稿 / 修正第 n 页分界 |
