# Playbooks

Put reusable task playbooks here.

Examples:

- onboarding a new repository
- preparing a release workspace
- reviewing a cross-repo change
- generating AI-ready context for a task
- adding an umbrella-level feature under `features/` — see `add-umbrella-feature.md`
